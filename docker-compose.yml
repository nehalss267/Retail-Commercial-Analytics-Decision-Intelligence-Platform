services:
  postgres:
    image: postgres:16-alpine
    container_name: retailai-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-retailai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-retailai_secret}
      POSTGRES_DB: ${POSTGRES_DB:-retailai}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-retailai}"]
      interval: 5s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: retailai-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@retailai.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin}
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
